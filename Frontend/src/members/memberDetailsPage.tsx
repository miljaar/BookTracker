/*
import { useParams } from "react-router-dom";

function readMemberId(memberId: string) {
    const
}

export function MemberDetailsPage() {
    const { memberId: memberIdParameter } = useParams();
    const memberId = Number(memberIdParameter)
    return (<></>);
}

*/